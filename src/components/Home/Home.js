import React, { Component } from 'react'

export default class Home extends Component {
    render() {
        return (
            // Every component musr return one and only one div
            <div>
                Home
            </div>
        )
    }
}
